# ClaimPilot Workspace Agents

This workspace defines three project agents to coordinate implementation across the frontend, backend, and database layers.

- `frontend` — implements UI, client-side routing, state, and API integration.
- `backend` — implements server API routes, OpenAI integration, and persistence services.
- `database` — defines the schema, migrations, and data model for incident reports and evidence.
